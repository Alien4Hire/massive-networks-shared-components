import { newSpecPage } from '@stencil/core/testing';
import { {{properCase componentName}} } from '../{{dashCase (nameSpace componentName)}}';

describe('{{dashCase componentName}}', () => {
  it('renders', async () => {
    const page = await newSpecPage({
      components: [{{properCase componentName}}],
      html: `<{{dashCase (nameSpace componentName)}}></{{dashCase (nameSpace componentName)}}>`,
    });
    expect(page.root).toEqualHtml(`
      <{{dashCase (nameSpace componentName)}}>
        <mock:shadow-root>
          <slot></slot>
        </mock:shadow-root>
      </{{dashCase (nameSpace componentName)}}>
    `);
  });
});