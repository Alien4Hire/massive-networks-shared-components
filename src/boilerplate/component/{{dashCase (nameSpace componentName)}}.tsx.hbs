import { Component, Host, h } from '@stencil/core';

@Component({
  tag: '{{dashCase (nameSpace componentName)}}',
  styleUrl: '{{dashCase (nameSpace componentName)}}.scss',
  shadow: true
})
export class {{properCase componentName}} {
  render() {
    return (
      <Host>
        <slot></slot>
      </Host>
    );
  }
}